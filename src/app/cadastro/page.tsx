import { PageLayoutAuth } from '@components/layout/auth/page';
import { RegisterBox } from '@components/organisms/auth/RegisterBox';

export default function Cadastro() {
  return (
    <PageLayoutAuth>
      <RegisterBox />
    </PageLayoutAuth>
  );
}
